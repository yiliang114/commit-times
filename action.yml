# https://help.github.com/en/articles/metadata-syntax-for-github-actions
name: 'Commit Numbers Today'
description: 'get someone commit numbers today'
author: 'yiliang114'
branding:
  color: 'purple'
  icon: 'aperture'

inputs:
  who-to-greet: # 输入 id
    description: 'Who to greet'
    required: true
    default: 'World'
  # username:
  #   description: 'GitHub user who you want to get commits numbers today'
  #   required: true
  #   default: 'yiliang114'

outputs:
  time: # 输出 id
    description: 'The time we greeted you'
  # sum: # id of the output
  #   description: 'The sum of the commits today'

runs:
  using: 'node12'
  main: 'index.js'
